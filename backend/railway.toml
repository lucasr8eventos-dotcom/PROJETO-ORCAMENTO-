[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate && npx prisma migrate deploy && npx tsx prisma/seed.ts"

[deploy]
startCommand = "npm start"
restartPolicyType = "on_failure"
