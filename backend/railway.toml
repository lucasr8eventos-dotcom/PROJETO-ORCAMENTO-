[build]
builder = "nixpacks"
buildCommand = "npm install && npm run build && npx prisma generate"

[deploy]
startCommand = "npx prisma migrate deploy && npx tsx prisma/seed.ts && npm start"
restartPolicyType = "on_failure"
